import { FcAttrDirective } from './fc-attr-directive.directive';

describe('FcAttrDirective', () => {
  it('should create an instance', () => {
    const directive = new FcAttrDirective();
    expect(directive).toBeTruthy();
  });
});
